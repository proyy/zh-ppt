# zh-ppt 技能 - Python 依赖
# 适用于所有 scripts/ 下的脚本

# OpenAI SDK（兼容 Qwen API）
openai>=1.0.0

# HTTP 请求库（ppt_generator.py, sync_config.py）
requests>=2.28.0

# 图片处理（generate.py）
Pillow>=9.0.0

# 环境变量加载（generate.py 读取 .env）
python-dotenv>=1.0.0
